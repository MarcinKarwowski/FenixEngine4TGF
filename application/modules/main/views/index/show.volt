<div class="news-row">
    <div class="news-title">{{ article.title }} <span style="float: right;">{{ article.getDate() }}</span></div>
    <div class="news-body">{{ article.getTextdata().text }}</div>
</div>